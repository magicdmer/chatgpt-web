# 插件分支需求与问题检查记录

本文档汇总本次对高级模式、Function Call 插件、生图能力、模型与 Key 选择、管理员权限、图片对话及开发环境的检查和沟通结论，作为后续实现与验收依据。

## 0. 项目用途

- 本项目由开发者个人开发并主要供自己使用，同时分享给少量亲友，不面向大规模用户或公开插件生态。
- 项目中开发者与管理员为同一人，不需要额外设计开发者审核、插件市场或多级发布流程。
- 插件由开发者编写并随服务部署。插件出现在服务端注册表中即视为已接入，不再单独维护“已注册”业务状态。

## 1. 项目定位与前提

- 本分支的核心目标是引入基于 OpenAI Function Call（`tools`）的内部插件能力。
- 模型服务统一通过 `new-api` 接入，但系统允许配置多组 API Key 和 `baseURL`。
- 纳入系统使用的聊天模型默认都支持 Function Call；不支持 `tools` 的模型不进入可用模型范围，暂不建设模型能力探测或兼容降级机制。
- 插件系统不是第三方插件市场，不支持普通用户安装或上传插件代码。
- 管理员可自用所有已接入插件，并决定是否将插件发布给其他用户。
- 普通用户可在设置中查看已发布插件并选择是否为自己启用；聊天时仍由模型根据自然语言自动调用。

## 2. 普通模式与高级模式

### 2.1 普通模式

高级模式默认关闭，面向不熟悉模型参数的普通用户，目标体验类似 ChatGPT 官方产品：用户直接输入需求，由系统自动选择模型并决定是否调用插件。

普通模式要求：

- 不在聊天输入区显示当前房间的模型选择器。
- 不显示手动图片生成按钮。
- 用户无需配置模型即可创建会话并正常聊天。
- 保留上下文开关。用户需要能够决定是否携带历史对话。
- 保留思考开关。不同模型不一定支持自动思考，仍需允许用户显式开启或关闭。
- Function Call 插件保持可用，由模型根据用户意图自动调用，不受高级模式开关影响。
- 图片上传与图文对话保持可用。

### 2.2 高级模式

高级模式面向了解模型及请求参数的用户。开启后至少提供：

- 当前房间模型选择。
- 手动图片生成入口。
- System Prompt、Temperature、Top P 等专业参数控制。
- 普通模式已有的上下文、思考、图片上传和自动插件调用能力。

高级模式属于浏览器本地偏好，当前存储于 `appSetting.advancedMode`。开关应在切换时立即持久化，不应依赖用户额外点击“保存用户信息”。

### 2.3 默认模型层级

系统存在两个不同层级的默认模型：

1. **全局默认模型**：由管理员在站点设置中配置，对全站生效。
2. **个人默认模型**：由当前用户在个人设置中配置，只影响该用户。

管理员同时具有管理员身份和普通用户身份，因此可以看到全局默认模型与自己的个人默认模型；普通用户只能看到个人默认模型。

新建会话的模型选择优先级保持为：

```text
个人默认模型
→ 全局默认模型
→ 当前用户可用模型列表中的第一个
→ 静态兜底模型
```

个人默认模型是可选偏好。普通模式的含义是“用户不需要选择模型”，不是禁止用户设置个人默认模型。

## 3. 插件系统

### 3.1 定位与权限

- 去掉独立的“插件商店”入口，在用户设置中增加“插件”Tab。
- 普通用户只能看到管理员已发布的插件；管理员可看到所有已接入插件。
- 普通用户的插件列表使用“状态”列展示已启用/未启用，并在“操作”列提供“启用/停用”按钮，不使用 Switch，也不提供插件参数设置。个人启用状态按用户保存，不影响其他用户。
- 管理员额外拥有“发布/取消发布”操作。发布决定其他用户能否看到并使用插件，不影响管理员自己使用。
- 管理员的插件表格额外显示“发布状态”，操作列按当前状态提供“启用/停用”、“设置”和“发布/取消发布”按钮。
- 插件的模型、服务参数及其他运行配置由管理员统一设置，对所有启用该插件的用户生效。
- 未发布插件仅管理员可见、可启用和测试；管理员的个人启停操作不得改变其他用户的使用状态。
- 普通用户的插件列表和个人启停接口需经过身份认证；发布、取消发布和插件配置接口还必须校验管理员权限。

### 3.2 插件来源

- 插件是服务端可信代码，不支持普通用户安装插件。
- 项目根目录下增加 `plugins/`，一个插件对应一个子目录；子目录至少包含 `plugin.json` 和一个 TypeScript 入口源文件。
- Docker 镜像中的插件路径统一为 `/app/plugins`，通过将宿主机项目的 `plugins/` 绑定挂载到 `/app/plugins` 提供插件源码。建议生产环境使用只读挂载。
- “添加插件”是指开发者在 `plugins/` 下新增插件目录并重启服务，不再手工修改中心插件注册表。
- 管理界面不提供上传、粘贴或动态执行任意代码的能力。
- 服务启动时自动扫描 `plugins/` 的直接子目录并建立内存注册表。成功加载的新插件默认可供管理员选择自用，但默认未启用、未发布。
- 新插件加载后应自动出现在管理员的插件 Tab，无需前端为每个插件硬编码列表逻辑。
- “已注册”只是代码层的客观前提，不作为需要在数据库或界面中维护的独立状态。
- 首期不实现热加载；插件源码或 `plugin.json` 变更后需重启服务，避免 TypeScript 模块缓存与正在执行的请求产生不一致。

### 3.3 插件定义

插件目录建议结构：

```text
plugins/
  image-generation/
    plugin.json
    index.ts
    lib/                 可选，插件内部 TypeScript 模块
```

`plugin.json` 只负责插件级元数据和设置 Schema，不再重复声明工具。Function Call 定义与对应执行方法统一放在 TypeScript 源码中，例如：

```json
{
  "manifestVersion": 1,
  "apiVersion": 1,
  "id": "ad373f21b9bd470ca761216e99d95d52",
  "name": "图片生成",
  "version": "1.0.0",
  "description": "根据文字描述生成或编辑图片",
  "author": "admin",
  "entry": "index.ts",
  "settings": {
    "model": {
      "description": "图片模型",
      "type": "model",
      "default": "dall-e-3"
    }
  }
}
```

`plugin.json` 对应类型至少包含：

```ts
interface PluginManifest {
  manifestVersion: 1
  apiVersion: 1
  id: string
  name: string
  version: string
  description: string
  author?: string
  entry: string
  settings: Record<string, PluginSettingDefinition>
}
```

- `id`：插件唯一标识，由插件源码固定声明。格式为去掉连字符的 UUID/GUID 并统一转为小写，例如 `ad373f21b9bd470ca761216e99d95d52`，一旦发布不得更改。
- `manifestVersion`：`plugin.json` 文件结构版本；`apiVersion`：插件 TypeScript 与宿主 `PluginContext` 的契约版本。宿主不支持时拒绝加载并给出明确错误。
- `name`：插件 Tab 中显示的插件名称，例如“图片生成”，不要求唯一。
- `version`：插件版本，使用语义化版本格式。
- `entry`：相对于插件目录的 TypeScript 入口，解析后路径必须仍位于当前插件目录内，禁止通过 `../` 访问目录外入口。
- `settings`：驱动管理员设置弹窗、生成默认值并用于服务端校验。普通用户接口不返回该定义或插件内部配置。
- 一个插件可以提供多个 Function Call 工具，但发布、个人启停和管理员设置仍以整个插件为单位，首期不提供单个工具的独立启停。

### 3.4 插件发布、个人启停与配置

插件发布状态与用户个人启用状态必须分开保存。建议支持：

```text
plugin_config
  id               TEXT 主键，32 位小写十六进制 GUID，不含连字符
  name             插件显示名称，不设数据库唯一约束
  published        是否发布给其他用户
  settings         管理员统一管理的全局配置 JSON

user_plugin_config
  user_id          用户 ID
  plugin_id        关联 plugin_config.id
  enabled          当前用户是否启用

plugin_storage
  plugin_id        关联 plugin_config.id
  scope            global 或 user
  owner_id         global 作用域为空字符串，user 作用域为用户 ID
  key              插件内部存储键
  value            JSON 值
  updated_at       更新时间

约束
  plugin_config.id                      PRIMARY KEY
  user_plugin_config(user_id, plugin_id) UNIQUE
  plugin_storage(plugin_id, scope, owner_id, key) UNIQUE
  user_plugin_config.user_id             FOREIGN KEY -> user.id ON DELETE CASCADE
  user_plugin_config.plugin_id           FOREIGN KEY -> plugin_config.id ON DELETE CASCADE
  plugin_storage.plugin_id               FOREIGN KEY -> plugin_config.id ON DELETE CASCADE
```

`id`/`plugin_id` 是插件唯一性和数据库关联的唯一依据；`name` 只是面向用户的插件显示名称，不设唯一约束；`toolName` 是模型调用工具和服务端查找执行器的技术标识，也不设数据库唯一约束。服务启动时应将注册表中尚未存在的插件写入 `plugin_config`，默认 `published=false`；如果同一 `id` 的源码 `name` 已修改，同步更新数据库中的显示名称。

运行时要求：

1. 服务端读取可信插件注册表，校验 `id` 符合 `^[0-9a-f]{32}$` 且不重复，校验每个插件的工具定义合法且插件内部不重名，再按 `id` 补齐 `plugin_config` 记录。
2. 管理员可看到注册表中的全部插件；普通用户只能查询 `published=true` 的插件元数据。
3. 管理员使用插件不受 `published` 限制；普通用户必须同时满足插件已发布且个人已启用。
4. 只把当前用户可用且 `enabled=true` 的插件展开为工具列表，并将其全部工具转换为 OpenAI `tools`。
5. 模型返回 `tool_calls` 后，必须在服务端按相同规则再次校验，不允许执行当前用户不可用的插件。
6. 插件配置只允许管理员更新，保存前必须按插件 Schema 校验。
7. 用户尚未写入个人记录时，插件默认为未启用。
8. 用户开启插件前，服务端必须检查该插件的每一个工具名是否与该用户其他已启用插件重复。任一工具冲突时拒绝启用整个插件，并返回冲突的工具名和插件名，不允许依赖列表顺序任意选择执行器。

本项目只使用支持 Function Call 的模型，因此无需为每个模型维护 `tools` 能力白名单。

### 3.5 Function Call 执行

当前实现已经具备以下基础能力，后续应保留：

- 向聊天请求注册工具定义。
- 拼接流式返回的 `tool_calls` 名称与参数。
- 支持同一轮多个工具调用。
- 将插件结果作为 `tool` 消息返回聊天模型。
- 限制单次请求的最大工具调用轮数，避免无限循环。
- 向前端增量返回插件调用状态。
- 插件执行失败时返回可理解的错误，并结束或继续当前调用链。

任何加入 `tools` 的工具都必须有对应的注册和执行实现。当前代码中临时加入的 `googleSearch` 没有对应执行器，后续应删除该声明或实现并注册对应插件，不能继续暴露不可执行工具。

### 3.6 插件设置 Schema

`plugin.json.settings` 是以设置键为 key 的对象。完整示例：

```json
{
  "api_key": {
    "description": "API 密钥",
    "type": "string",
    "hint": "用于调用外部服务。",
    "default": "",
    "required": true,
    "secret": true,
    "obvious_hint": true
  },
  "daily_limit": {
    "description": "每个用户的每日使用限额",
    "type": "int",
    "default": 10,
    "min": 0,
    "max": 1000
  },
  "enabled_groups": {
    "description": "启用的 QQ 群号列表",
    "type": "list",
    "item_type": "string",
    "default": []
  }
}
```

设置定义建议支持：

```ts
interface PluginSettingDefinition {
  description: string
  type: 'string' | 'text' | 'int' | 'float' | 'boolean' | 'list' | 'select' | 'model'
  hint?: string
  obvious_hint?: boolean
  default: unknown
  required?: boolean
  secret?: boolean
  placeholder?: string
  min?: number
  max?: number
  pattern?: string
  item_type?: 'string' | 'int' | 'float'
  options?: Array<{ label: string, value: string | number }>
}
```

- `description`：设置项显示名称。
- `hint`：作为输入控件的占位提示使用，避免在紧凑表单中额外占一行；`model` 类型不显示插件定义的 `hint`，由宿主根据模型列表是否为空显示选择提示或引导管理员前往 Keys 管理刷新。
- `obvious_hint`：字段暂时保留在 Schema 中，但当前界面不单独渲染说明文本；以后确有复杂配置需要时再统一设计 Tooltip 或帮助区域。
- `default`：数据库尚未保存该字段时的值，必须与 `type` 一致。
- `required`、`min`、`max`、`pattern`、`item_type` 同时用于前端输入校验和服务端强制校验。
- `secret=true` 时前端使用密码输入框，服务端不在查询接口中回传明文；编辑时留空表示保留原值，如需清空应提供明确操作。
- `select` 使用静态 `options`；`model` 由宿主系统动态注入当前可配置模型列表，用于生图模型等选择项。
- `list` 首期支持简单标量数组，前端使用动态输入列表；暂不支持嵌套对象列表。
- 配置保存到 `plugin_config.settings` 时只保存值，Schema 始终以 `plugin.json` 为准。插件升级后新增字段自动使用默认值，未知旧字段不再传入插件运行上下文。
- `daily_limit` 等字段的业务含义由插件源码实现，宿主系统只负责展示、校验和传入配置，不根据字段名自动实施限额。

### 3.7 TypeScript 入口与加载器

TypeScript 入口默认导出一个继承 `BasePlugin` 的类。插件开发者不需要手写 `getTools()`：每个作为 Function Call 工具的方法使用 `@llmTool(...)` 装饰器标记，基类自动收集并返回工具。例如：

```ts
import { BasePlugin, llmTool } from '@chatgpt-web/plugin-sdk'
import type { PluginContext } from '@chatgpt-web/plugin-sdk'

interface GenerateImageArgs {
  prompt: string
}

interface EditImageArgs {
  imageUrl: string
  prompt: string
}

export default class ImagePlugin extends BasePlugin {
  @llmTool({
    name: 'generate_image',
    description: 'Generate an image from a text prompt.',
    parameters: {
      type: 'object',
      properties: {
        prompt: { type: 'string', description: 'The image prompt.' },
      },
      required: ['prompt'],
    },
  })
  async generateImage(args: GenerateImageArgs, context: PluginContext): Promise<string> {
    return await context.services.images.generate({
      prompt: args.prompt,
      model: String(context.settings.model ?? ''),
    })
  }

  @llmTool({
    name: 'edit_image',
    description: 'Edit an existing image from a text instruction.',
    parameters: {
      type: 'object',
      properties: {
        imageUrl: { type: 'string', description: 'The source image URL.' },
        prompt: { type: 'string', description: 'The edit instruction.' },
      },
      required: ['imageUrl', 'prompt'],
    },
  })
  async editImage(args: EditImageArgs, context: PluginContext): Promise<string> {
    return await context.services.images.edit({
      imageUrl: args.imageUrl,
      prompt: args.prompt,
      model: String(context.settings.model ?? ''),
    })
  }
}
```

对插件开发者而言，“一个被 `@llmTool` 标记的方法就是一个工具”。新增工具只需新增方法，不需要同时修改 `plugin.json`、`tools: []` 和分发 `switch`。

SDK 对外契约：

```ts
interface LlmToolDefinition {
  name: string
  description: string
  parameters: Record<string, any>
}

interface RegisteredTool extends LlmToolDefinition {
  methodName: string
  execute(args: Record<string, any>, context: PluginContext): Promise<string>
}

declare function llmTool(definition: LlmToolDefinition): MethodDecorator

abstract class BasePlugin {
  getTools(): RegisteredTool[]
}
```

- `BasePlugin.getTools()` 由 SDK 统一实现，自动扫描实例原型上带工具元数据的方法，并将方法绑定到当前插件实例。插件不应重写该方法。
- `@llmTool` 使用自定义 `Symbol` 将元数据记录在方法上，不引入 `reflect-metadata`，避免额外的反射依赖。
- 工具方法统一接收 `(args, context)` 两个参数，返回 `Promise<string>`。宿主在调用方法前按 `parameters` JSON Schema 校验 `args`。
- 同一插件内的工具名必须唯一，否则整个插件加载失败。不同插件可以存在同名工具，但同一用户不能同时启用这些插件。
- 插件启用后，其 `getTools()` 返回的全部工具都会发送给模型；首期不支持仅启用插件中的部分工具。

宿主提供的最小上下文：

```ts
interface PluginContext {
  pluginId: string
  user: { id: string, root: boolean }
  settings: Readonly<Record<string, any>>
  signal: AbortSignal
  logger: {
    info(message: string, data?: unknown): void
    warn(message: string, data?: unknown): void
    error(message: string, data?: unknown): void
  }
  storage: {
    get<T>(key: string, scope?: 'global' | 'user'): Promise<T | null>
    set<T>(key: string, value: T, scope?: 'global' | 'user'): Promise<void>
    delete(key: string, scope?: 'global' | 'user'): Promise<void>
  }
  services: {
    images: {
      generate(input: { prompt: string, model: string }): Promise<string>
      edit(input: { imageUrl: string, prompt: string, model: string }): Promise<string>
    }
  }
}
```

- `storage` 必须自动按 `pluginId` 分区；`user` 作用域还需加入当前 `user.id`，插件不能读写其他插件或其他用户的命名空间。
- 用户中止聊天时将同一 `AbortSignal` 传入插件；插件调用外部服务时应传递该 signal。
- 生图等宿主已有能力通过 `context.services` 提供，避免插件直接导入宿主 SQLite 或内部 Key 表。
- 服务端应提供稳定的 `@chatgpt-web/plugin-sdk` 包，至少导出 `BasePlugin`、`llmTool` 和公共类型。插件只依赖 SDK 公开接口，不通过相对路径导入主项目内部文件。
- 首期插件只允许使用 Node.js 内置模块、Node 18 全局 `fetch` 和相对路径下的插件内部模块，不在容器启动时为每个插件动态执行 `npm install`。
- Node.js 18 不能原生执行 TypeScript。服务端应将 `tsx` 作为生产依赖和统一运行器，由它动态导入挂载目录中的 `.ts` 入口，不依赖 Node 直接加载 TS。
- 项目 `tsconfig` 需明确开启与 SDK 装饰器实现匹配的 decorator 编译选项，并确保本地与 Docker 中的 `tsx` 读取同一份配置。
- 插件代码由开发者自己编写，不运行于安全沙箱中，与主服务共享进程权限。因此不得加载不可信的第三方插件。

加载流程：

1. 扫描 `PLUGIN_DIR` 指定目录；本地开发默认查找项目根目录 `plugins/`，Docker 显式设置为 `/app/plugins`，只处理包含 `plugin.json` 的直接子目录。
2. 解析并校验 manifest 版本、API 版本、ID、必填字段和设置 Schema。
3. 确认 `entry` 为插件目录内存在的 `.ts` 文件，动态导入默认导出，校验其是 `BasePlugin` 子类并创建实例。
4. 调用基类实现的 `getTools()`，要求至少存在一个工具，逐个校验工具名、描述、参数 JSON Schema 和对应方法，并拒绝插件内部重名工具。
5. 按 `id` 建立插件内存注册表，并同时建立工具名到执行方法的索引。工具索引在每次请求中按当前用户的已启用插件生成。
6. 同步 `plugin_config`；同一 ID 重复时两者都不加载，避免结果取决于文件扫描顺序。
7. 单个插件加载失败不得阻止整个聊天服务启动；记录包含插件目录和原因的错误日志，并在管理员插件 Tab 中将可识别的失败插件标记为“加载失败”，不允许启用或发布。
8. 插件目录被移除时，原数据库配置保留但不再可用；以后放回相同 ID 的插件时可恢复原有发布、配置和用户启停记录。

## 4. 生图插件

### 4.1 用户体验

- 普通模式下，用户直接表达“生成一张图片”等意图，由聊天模型自动调用生图插件。
- 高级模式下，用户既可以让模型自动调用生图插件，也可以使用手动图片生成入口。
- 用户勾选手动绘图后，该次请求视为强制生图：直接调用生图插件一次，不再同时请求聊天模型触发 Function Call，避免重复绘制。
- 生图结果以 Markdown 图片形式进入助手回复，并正常显示在聊天历史中。
- 管理员可在插件 Tab 中设置生图模型、选择自己是否启用，并决定是否发布给其他用户。

### 4.2 根据生图模型选择 Key

旧版生图插件复用了聊天模型选中的 Key，不符合多 Key、多 `baseURL` 场景；本轮已经改为根据管理员配置的生图模型重新选择 Key，当前实现遵循以下规则。

推荐选择规则：

1. 读取生图插件配置中的模型名称。
2. 从未禁用且当前有效的 Key 中查找支持该模型的候选项。
3. 优先使用 `availableModels` 判断 new-api 实际支持的模型；为兼容旧数据，可以回退检查 `chatModels`。
4. 继续遵守现有 Key 状态和用户角色约束，避免绕过权限。
5. 如果多个 Key 都支持该模型，按确定性顺序取第一个，建议按数据库 `id` 升序。
6. 使用选中 Key 对应的 `key` 与 `apiBaseUrl` 调用 new-api 图片生成接口。
7. 没有候选 Key 时，返回明确的“未找到支持该生图模型的 Key”错误。

`apiBaseUrl` 在拼接图片生成路径前应先规范化，兼容配置值带或不带末尾 `/`、`/v1` 的情况，避免重复路径。

## 5. 图片对话约束

- 专门的图片编辑按钮继续保留在代码中，但通过 `showEditButton = false` 默认关闭和隐藏。
- 当前前端所有带图片的常规请求统一走 `/chat-process`，按图文对话处理。
- 架构文档不再展开专用图片编辑流程，避免将“看图”和“改图意图”混为一谈。
- 历史图片自动回填只决定图片来源和文本上下文是否保留，不代表系统识别了用户的看图或改图意图。
- 启用上下文且当前没有新附件时：优先回填最近助手回复中的图片；否则回填最近历史请求中的图片附件。

## 6. 当前修改进度

### 6.1 当前里程碑

- 插件架构第一版、图片生成插件迁移、插件管理界面和用量统计重构已经完成。
- 对应主提交为 `97be101 feat: 重构插件系统和用量统计`。
- 当前实现仍定位为个人及亲友使用，不包含插件上传、第三方市场、在线安装、热加载或独立进程沙箱。

### 6.2 已完成：插件运行时

- 已增加项目内置的 `@chatgpt-web/plugin-sdk`，提供 `BasePlugin`、`@llmTool`、`PluginContext`、日志、隔离存储和宿主图片服务类型。
- 已实现 `plugins/<plugin-folder>/plugin.json + index.ts` 文件系统扫描，使用生产依赖 `tsx` 动态加载 TypeScript 入口。
- 已实现 manifest 版本、API 版本、32 位小写 GUID、语义化版本、入口路径、设置 Schema、工具名称、工具参数 JSON Schema 及插件内部重名校验。
- 已实现一个插件包含多个 `@llmTool` 方法的自动收集机制，插件开发者不需要维护重复的 `tools: []` 清单或工具分发 `switch`。
- 已删除旧的 `service/src/chatgpt/plugins` 硬编码注册数组和没有执行器的临时工具声明。
- 已将图片生成迁移为 `plugins/image-generation`，固定插件 ID 为 `ad373f21b9bd470ca761216e99d95d52`。
- 单个带有可识别 ID 的插件加载失败不会阻止服务启动；管理员列表会显示加载错误并禁止启用和发布。

### 6.3 已完成：发布、启停、配置与存储

- `plugin_config.id` 已改为插件声明的文本 GUID，并增加 `published`；`name` 不再承担唯一标识职责。
- 已增加 `user_plugin_config` 保存每个用户独立的启用状态，增加 `plugin_storage` 提供按插件、全局或用户隔离的 JSON 存储。
- 已实现旧整数插件配置表迁移；旧 `generate_image` 配置会映射到固定图片插件 ID，原生图模型设置能够保留。
- 管理员可查看、设置、启停和发布全部已加载插件；未发布插件仍可由管理员个人启用测试。
- 普通用户只能查看已发布插件，只能执行个人启用或停用，不能读取 Schema、内部设置或调用管理员接口。
- 启用插件时已实现工具名冲突检查；发生冲突会拒绝整个启用操作并返回工具名及冲突插件名。
- 插件设置弹窗已支持 `string`、`text`、`int`、`float`、`boolean`、`list`、`select` 和 `model` 控件，服务端会再次执行强制校验。
- `secret=true` 的值不会由查询接口返回明文；留空保存会保留已有密钥。
- `model` 设置在打开弹窗时直接读取正常 Key 已持久化的 `availableModels` 并集，不请求外部服务，也不回退到静态默认模型；列表为空时引导管理员先到 Keys 管理刷新。

### 6.4 已完成：聊天和图片生成链路

- 聊天请求只向模型发送当前用户已启用且有权使用的插件工具；收到 `tool_calls` 后会按相同规则重新校验执行权限。
- 保留了流式工具调用拼接、同轮多个调用、最多五轮调用、工具结果回传和前端调用状态提示。
- 手动绘图直接调用当前用户启用的 `generate_image` 工具并立即返回，不再进入聊天模型 Function Call 流程，因此一次请求不会触发两次绘图。
- 自动 Function Call 在普通模式和高级模式中均保留；高级模式的手动绘图按钮只负责强制选择本次请求的执行路径。
- 图片生成会按管理员选择的生图模型重新筛选正常 Key，遵守用户角色，优先检查 `availableModels`，兼容回退 `chatModels`，并按 Key ID 确定性选择。
- 已规范化 Key 的 `apiBaseUrl`，兼容末尾斜杠和 `/v1`；没有可用 Key、响应错误或缺少图片 URL 时返回明确错误。

### 6.5 已完成：界面、部署与其他修复

- 已移除侧边栏独立“插件商店”，在设置中增加所有登录用户可见的“插件”Tab，使用与用户管理一致的状态列和操作按钮。
- Docker 镜像包含插件 SDK 和内置插件；Compose 将宿主机 `plugins/` 只读挂载到 `/app/plugins`，并设置 `PLUGIN_DIR=/app/plugins`。
- 前端开发端口由 `VITE_APP_PORT` 配置，默认从无权限的 `1002` 调整为 `10002`。
- 管理员新增用户时已增加前后端邮箱格式和密码非空校验，邮箱保存前会去空格并转为小写。
- 用量统计页已重构为请求次数、Token 总量、平均每次、估算占比、每日 Token 流向、用户排行和模型分布；普通用户只能查看本人，管理员可查看全站或指定用户。
- 统计日期改为服务器本地时区聚合，单次查询限制为 366 天，并补充错误提示、空状态、深浅色和移动端响应式布局。
- 已同步更新 `docs/ARCHITECTURE.md`，并将 `service/data/` 加入 Git 忽略，避免提交本地 SQLite 数据。

### 6.6 已完成验证

- 前端 `vue-tsc --noEmit` 通过，前端生产构建通过。
- 后端 `tsc --noEmit` 和 `tsup` 构建通过。
- 插件目录扫描、TypeScript 动态导入、图片生成插件识别和服务启动冒烟验证通过。
- 旧 `plugin_config` 整数表迁移验证通过，旧生图模型设置保留。
- 统计查询已使用现有 SQLite 数据验证全站汇总、每日数据、用户排行和模型分布。
- Docker Compose 配置解析通过。
- 浏览器自动截图验证尚未执行：当前环境没有 Playwright 与 Chromium，未为此向项目引入额外依赖。

### 6.7 后续待办

- 为插件加载器、多个工具收集与路由、工具名冲突、发布权限、Schema、存储隔离和多 Key 失败路径增加正式自动化测试。目前只有类型检查、构建、迁移和运行冒烟验证。
- 完善无法从损坏 manifest 中解析出合法插件 ID 时的管理员错误展示；当前此类错误只记录服务端日志。
- 为 `secret` 设置增加显式“清空已保存值”操作；当前空字符串表示保留旧值。
- 根据后续真实插件需求扩充 `PluginContext.services`；图片编辑宿主服务目前仍明确返回“尚未开放”。
- 评估是否持久化 `tool_calls` 和工具结果，以便跨轮历史恢复完整工具上下文。
- 首期仍不实现插件热加载、第三方依赖自动安装、插件上传、插件市场和不可信代码沙箱。

## 7. 验收标准

### 7.1 普通模式

- 新用户首次进入时高级模式为关闭状态。
- 聊天输入区不显示模型选择和手动图片生成按钮。
- 用户未设置个人默认模型时，仍能使用全局默认模型正常创建会话。
- 上下文、思考、图片上传和自动插件调用可以正常使用。
- 用户输入明确生图需求时，支持 Function Call 的聊天模型能够自动调用已启用的生图插件。

### 7.2 高级模式

- 开关切换后立即持久化，刷新页面状态不丢失。
- 开启后显示当前房间模型选择、手动图片生成及专业参数。
- 关闭后隐藏专业控制，但不影响上下文、思考、图片上传和自动插件调用。

### 7.3 插件管理

- 设置中存在“插件”Tab，列表样式与现有用户管理表格统一，包含插件名称、状态和操作列。普通用户的操作列只显示“启用/停用”按钮，不显示设置按钮或插件内部配置。
- 普通用户只能查看已发布插件，不能访问未发布插件或管理配置。
- 管理员可查看全部已接入插件，可个人启用未发布插件进行自用和测试。
- 管理员可看到插件设置按钮，修改后的配置对所有启用该插件的用户统一生效。
- 管理员发布插件后，普通用户才能看到并为自己启用；取消发布后，普通用户的新请求不得再获得该工具。
- 任一用户切换个人启用状态时，不影响其他用户。
- 用户停用插件后，其新聊天请求不再向模型发送该插件定义。
- 当用户尝试启用包含冲突工具名的插件时，服务端拒绝保存整个插件的启用状态，列表仍显示未启用，并弹出“无法启用，工具 xxx 与插件 xxx 重复”消息。
- 非法插件 ID、插件内部重名或格式非法的工具名、不在服务端注册表中的插件和不符合 Schema 的配置无法保存或执行。
- 一个插件类中定义多个 `@llmTool` 方法后，启用该插件会将所有工具正确发送给模型，模型返回任一工具名时都能路由到对应方法。
- 新增可信插件并部署后，管理员的插件 Tab 能根据元数据自动展示，且默认未发布。
- 将包含合法 `plugin.json` 和 TypeScript 入口的目录挂载到 `/app/plugins` 并重启后，插件能够被自动发现和加载，无需修改主服务注册代码。
- 单个插件 manifest、设置 Schema 或 TypeScript 入口错误时，主服务仍能启动，管理员可看到加载失败状态和原因。
- 管理员设置弹窗能够按 `string`/`int`/`boolean`/`list`/`select`/`model` 等类型自动生成控件，且非法值无法绕过服务端校验。
- `secret=true` 的设置值不会由查询接口明文返回给前端。

### 7.4 生图插件

- 管理员配置生图模型后，系统能从多组 Key/`baseURL` 中找到支持该模型的候选 Key。
- 多个候选 Key 存在时，始终选择排序后的第一个，行为可重复验证。
- 聊天 Key 不支持生图模型时，不影响插件使用其他正确的 Key。
- 没有可用 Key、new-api 返回错误或图片 URL 缺失时，用户能看到明确错误信息。
- 当前用户停用生图插件后，模型不能再为该用户自动调用生图插件。
- 手动绘图每次只执行一次生图插件，不会再同时触发聊天模型的生图 Function Call。

## 8. 开发环境

- 项目统一使用 Node.js `18.20.8`。
- 使用 pnpm `9.15.9`，与当前 `lockfileVersion: '9.0'` 匹配。
- 根目录 `mise.toml` 统一管理 Node.js 和 pnpm；进入项目根目录或 `service/` 后应自动切换版本。
- 全局环境可以继续使用其他 Node.js 版本，不应覆盖项目级 mise 配置。
- 本地与 Docker 统一使用 `PLUGIN_DIR` 指定插件根目录，本地开发默认为项目根目录的 `plugins/`，Docker 中默认为 `/app/plugins`。
- Docker Compose 建议增加只读挂载，具体宿主机相对路径按 Compose 文件位置调整：

```yaml
services:
  app:
    volumes:
      - ../plugins:/app/plugins:ro
    environment:
      PLUGIN_DIR: /app/plugins
```

## 9. 实施清单

- [x] 定义 `plugin.json` manifest 和 `@chatgpt-web/plugin-sdk`，实现 `BasePlugin`、`@llmTool`、`PluginContext` 及工具参数校验。
- [x] 引入生产依赖 `tsx` 并配置 TypeScript decorators，实现文件系统扫描、插件类导入、多工具收集、校验与错误隔离。
- [x] 将现有生图插件迁移到 `plugins/image-generation/`，固定 32 位小写 GUID，并清理旧的硬编码注册和无执行器工具声明。
- [x] 增加 `published`、用户个人启用表和 `plugin_storage`，完成旧整数 ID 及生图配置数据迁移。
- [x] 改造插件查询、发布、个人启停、多工具名冲突、配置校验与按用户过滤工具的逻辑。
- [x] 去掉“插件商店”，在设置中实现 Schema 驱动的“插件”Tab 和管理员设置弹窗。
- [x] 重构生图的 Key/`baseURL` 选择逻辑，让手动绘图复用插件工具方法，并完善普通/高级模式边界。
- [ ] 补充插件加载、多工具收集与路由、Schema、发布、个人启停、调用权限、存储隔离、多 Key 和失败路径的正式自动化测试。
- [x] 同步更新 `docs/ARCHITECTURE.md` 和包含多工具的插件开发示例。
